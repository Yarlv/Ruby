# Ассоциативные массивы проходил? Если да, то третье задание сразу:
# Входные данные: массив пар.
# Результат: ассоциативный массив, составленный из этих пар так,
# что первый элемент пары — становится ключём, второй элемент — значением.#

arr = [[1,2],[3,4],['a','b']]

def ArrRev (arr_in)
  arr_in = Hash[*arr_in.flatten]
  return arr_in
end

a = ArrRev(arr)

puts a.to_s
